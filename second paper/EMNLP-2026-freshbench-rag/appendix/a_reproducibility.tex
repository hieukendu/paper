\section{Reproducibility Checklist}
The final package should include dataset versions, source licenses, prompts, API model names and dates, random seeds, preprocessing scripts, cached outputs, metric implementations, and per-example traces.

Current generated artifacts are placeholders. The files in \texttt{results/} and \texttt{figures/} are produced by \texttt{scripts/make\_artifacts.py} and are labelled \syntheticlabel{}.
