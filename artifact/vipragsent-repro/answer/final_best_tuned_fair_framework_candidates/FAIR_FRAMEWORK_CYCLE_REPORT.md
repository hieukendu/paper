# ViPragSent Fair-Framework Cycle

The cycle stopped during mandatory initial verification. Imported baseline raw predictions do not reproduce every registered score within `1e-6`; no calibration, gate, retraining, source selection, or candidate-test evaluation was performed.

The authoritative `final_best_tuned` directory and all baseline artifacts were left unchanged.

NOT_PROMOTED
